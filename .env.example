# WAJIB — Database MySQL

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=
DB_NAME=db_wa_blast

# WAJIB — Keamanan login dan akun admin
JWT_SECRET=buat_string_random_minimal_32_karakter_disini
SUPERADMIN_USERNAME=superadmin
SUPERADMIN_PASSWORD=ganti_password_minimal_12_karakter

# OPSIONAL — Runtime license untuk deployment berlisensi / managed deployment.
# Isi hanya jika build production memakai license gate sesuai manifest handover.

LICENSE_KEY=
LICENSE_API_SECRET=
LICENSE_OWNER=
LICENSE_EMAIL=
LICENSE_ORDER_ID=
LICENSE_API_URL=
# Public key untuk memverifikasi respons server lisensi, jika license gate dipakai.
LICENSE_RESPONSE_SIGNING_PUBLIC_KEY=
LICENSE_SIGNATURE_MAX_AGE_SECONDS=300
LICENSE_PUBLIC_RESET_ENABLED=false

# Build production berlisensi opsional dengan URL dan public key tetap:
# go build -ldflags "-X wa-assistant/backend/license.PinnedLicenseAPIURL=https://license.example.com -X wa-assistant/backend/license.PinnedLicenseSigningPublicKey=BASE64_PUBLIC_KEY"

# OPSIONAL — Server dan batas penggunaan

PORT=3030
DB_PATH=./wa-assistant.db
CORS_ALLOWED_ORIGINS=*
TOKEN_TTL_HOURS=24
LOGIN_WINDOW_MIN=10
LOGIN_LOCK_MIN=10
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=5
MEDIA_RETENTION_DAYS=30
WA_MEDIA_MAX_MB=16
API_RATE_PER_MIN=60
API_RATE_BURST=20

# OPSIONAL — AI. Pengaturan utama dapat diisi dari dashboard
OPENROUTER_API_KEY=
OPENROUTER_MODEL=deepseek/deepseek-chat

# OPSIONAL — Cek ongkir (Mengantar API — lebih lengkap: ada booking + tracking + auto-resi)
MENGANTAR_API_KEY=
# Alamat asal pengiriman (ID dari app.mengantar.com → Alamat → PICKUP_AUTOFILL)
MENGANTAR_ORIGIN_AUTOFILL_ID=
# ID saved address untuk origin pickup (dari GetMyAddresses)
MENGANTAR_ORIGIN_ADDRESS_ID=
# OPSIONAL — Cek ongkir via RajaOngkir (fallback)
RAJAONGKIR_API_KEY=

# OPSIONAL — Ongkir
# Diskon ongkir untuk customer yang pilih transfer (dalam Rupiah)
SHIPPING_TRANSFER_DISCOUNT=3000
# Berat default paket dalam gram
SHIPPING_DEFAULT_WEIGHT_GRAM=1000

# OPSIONAL — DeepSeek Direct (lebih murah, tanpa perantara OpenRouter)
DEEPSEEK_API_KEY=
